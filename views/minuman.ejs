<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>

    <!-- Bootstrap CSS -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/5.3.0/css/bootstrap.min.css" rel="stylesheet">
    <!-- Tailwind CSS -->
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
    <div class="container my-5">
        <h1 class="text-3xl font-bold mb-4 text-center text-blue-600">MINUMAN CAFE</h1>
        <h2 class="text-2xl font-semibold mb-4 text-center">Daftar Menu Minuman</h2>

        <table class="table table-striped table-bordered shadow-lg">
            <thead class="bg-blue-500 text-white text-center">
                <tr>
                    <th>Nama Minuman</th>
                    <th>Varian</th>
                    <th>Harga</th>
                </tr>
            </thead>
            <tbody>
                <% daftarMinuman.forEach(minuman => { %>
                    <tr class="text-center">
                        <td><%= minuman.NamaMinuman %></td>
                        <td><%= minuman.Varian %></td>
                        <td>Rp <%= minuman.Harga %></td>
                    </tr>
                <% }); %>
            </tbody>
        </table>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/5.3.0/js/bootstrap.bundle.min.js"></script>
</body>
</html>
